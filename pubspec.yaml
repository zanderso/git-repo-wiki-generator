name: git_repo_wiki_generator
description: A sample command-line application with basic argument parsing.
version: 0.0.1
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.8.0-70.0.dev

# Add regular dependencies here.
dependencies:
  args: ^2.5.0
  github: ^9.25.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
